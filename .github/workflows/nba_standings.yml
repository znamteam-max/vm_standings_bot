name: NBA standings to Telegram

on:
  workflow_dispatch:
  schedule:
    - cron: "5 5 * * *"  # каждый день ~07:05 по Европе/Хельсинки (cron работает в UTC)

permissions:
  contents: write   # нужно, чтобы коммитить файл с «вчерашними» позициями

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0    # чтобы можно было коммитить и пушить обратно

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests

      - name: Syntax check
        run: python -m py_compile nba_standings_bot.py

      - name: Run bot
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: python nba_standings_bot.py

      - name: Commit updated previous positions
        run: |
          if [ -n "$(git status --porcelain data/nba_prev_positions.json 2>/dev/null)" ]; then
            git config user.name "github-actions[bot]"
            git config user.email "github-actions[bot]@users.noreply.github.com"
            git add data/nba_prev_positions.json
            git commit -m "Update previous NBA standings [skip ci]" || true
            git push
          else
            echo "No changes to previous positions."
          fi
